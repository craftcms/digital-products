name: ci
on:
  push:
    branches:
      - develop
      - 'testing-workflow'
  pull_request:
    branches:
      - develop
      - 'testing-workflow'
jobs:
  workflow:
    name: Workflow
    uses: craftcms/.github/.github/workflows/plugins.yml@plugins-workflow
    with:
      craft_version: '3'