name: ci
on:
  push:
    branches:
      - develop
      - 'testing-workflow'
  pull_request:
    branches:
      - develop
      - 'testing-workflow'
jobs:
  ecs:
    name: ECS
    uses: craftcms/.github/.github/workflows/ecs.yml@plugins-workflow
    with:
      craft_version: '3'