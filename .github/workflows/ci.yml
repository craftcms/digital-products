name: ci
on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - 'testing-workflow'
  pull_request:
    branches:
      - develop
      - 'testing-workflow'
jobs:
  ci:
    name: CI
    uses: craftcms/.github/.github/workflows/plugins.yml@plugins
    with:
      craft_version: '3'